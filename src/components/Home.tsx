import React, { FunctionComponent } from 'react';

const Home: FunctionComponent = (props) => {
  return <div className="bg-gray-900 w-full h-screen m-0 flex flex-row"></div>;
};

export default Home;
